import { attendanceRelations, attendanceTable } from "./attendance.schema";
import { employeeRelations, employeeTable } from "./employee.schema";
import { invoiceLineRelations, invoiceLineTable } from "./invoice-line.schema";
import { invoiceRelations, invoiceTable } from "./invoice.schema";
import { memberRelations, memberTable } from "./member.schema";
import { membershipRelations, membershipTable } from "./membership.schema";
import { membershipPlanRelations, membershipPlanTable } from "./memberships-plan.schema";
import { productRelations, productTable } from "./product.schema";
import { sessionTable, userRelations, userTable } from "./user.schema";

export default {
  attendanceRelations,
  attendanceTable,
  employeeRelations,
  employeeTable,
  invoiceLineRelations,
  invoiceLineTable,
  invoiceRelations,
  invoiceTable,
  memberRelations,
  memberTable,
  membershipRelations,
  membershipTable,
  membershipPlanRelations,
  membershipPlanTable,
  productRelations,
  productTable,
  sessionTable,
  userRelations,
  userTable,
};
